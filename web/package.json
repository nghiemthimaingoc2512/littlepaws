{
  "name": "little-paws-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Little Paws — playable web build.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node tests/interaction.test.mjs",
    "artifact": "npm run build && node scripts/make-artifact.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^6.0.7",
    "vite-plugin-singlefile": "^2.1.0",
    "wait-on": "^9.1.0"
  }
}
